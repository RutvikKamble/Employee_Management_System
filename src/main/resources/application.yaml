server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_management_db
    username: root
    password: ""

  jpa:
    hibernate:
    ddl-auto: update   # auto sync with table
    srmat-sql: true